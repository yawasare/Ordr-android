<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="productivity.yaw.asare.ordr" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/lightgrey" >

        <activity
            android:name="productivity.yaw.asare.ordr.SplashActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="productivity.yaw.asare.ordr.MainActivity"
            android:label="@string/app_name">
        </activity>

        <receiver android:name="NotificationReceiver">
            <intent-filter>
                <action android:name="com.yaw.notify.push" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
